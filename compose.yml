volumes:
  pg-database:
    driver: local

services:
  ec-cube:
    ### ローカルでビルドする場合はこのblockを使い、下のimage行はコメントアウトしてください
    ## build:
    ##   context: .
    ##   args:
    ##     ECCUBE_VERSION: ${ECCUBE_VERSION:-latest}
    image: ${REGISTRY:-ghcr.io}/${IMAGE_NAME:-5ym/ec-cube}:${TAG:-latest}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - 8080:80
    environment:
      APP_ENV: "dev"
      APP_DEBUG: 1
      DATABASE_URL: "postgres://dbuser:secret@postgres/eccubedb"
      DATABASE_SERVER_VERSION: 14
      DATABASE_CHARSET: 'utf8'
      MAILER_DSN: "smtp://mailcatcher:1025"
      # ローカル検証専用のダミー値(本番では必ず固有の値に変更してください)
      ECCUBE_AUTH_MAGIC: "01701a3ce1a24bd9689bb20a2f22b7c1633f824022148627ce531457ac4c53de"
      ECCUBE_ADMIN_USER: "admin"
      ECCUBE_ADMIN_PASS: "password"
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: eccubedb
    ports:
      - 15432:5432
    volumes:
      - pg-database:/var/lib/postgresql/data
    healthcheck:
      test: pg_isready -U dbuser
      interval: 3s
      timeout: 3s
      retries: 3
  mailcatcher:
    image: schickling/mailcatcher
    ports:
      - 1080:1080
      - 1025:1025
